# BILC_DASHBOARD

CSV 파일의 행/열 정보와 컬럼 목록을 확인할 수 있는 간단한 Flask 대시보드입니다.

## 실행 방법

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

브라우저에서 `http://localhost:5000`으로 접속하면 대시보드를 확인할 수 있습니다.
